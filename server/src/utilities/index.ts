import wait from '@root/src/utilities/wait'
import sendResponse from '@root/src/utilities/sendResponse'
import isValidObjectId from '@root/src/utilities/isValidObjectId'

const utilities = {
  wait,
  sendResponse,
  isValidObjectId,
}

export default utilities
