type Genre {
  id: ID!
  name: String!
  color: String!
}

type Movie {
  id: ID!
  title: String!
  year: Int!
  myRating: Int!
  genres: [Genre!]!
  image: String!
  watchedAt: String!
}

type Query {
  movies: [Movie!]!
}
